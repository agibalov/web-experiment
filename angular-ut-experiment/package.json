{
  "name": "angular-ut-experiment",
  "version": "1.0.0",
  "description": "npm install -g karma # do I need to install this one globally at all? npm install -g karma-cli",
  "main": "index.js",
  "scripts": {
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js --single-run"
  },
  "author": "loki2302",
  "license": "ISC",
  "devDependencies": {
    "jasmine-core": "^2.1.3",
    "karma": "^0.12.31",
    "karma-chrome-launcher": "^0.1.7",
    "karma-jasmine": "^0.3.4"
  }
}
